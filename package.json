{
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "install:all": "npm install && concurrently \"npm install --prefix backend\" \"npm install --prefix frontend\""
  },
  "dependencies": {
    "all": "^0.0.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
